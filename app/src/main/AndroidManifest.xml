<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.impostertools.mvpcamera">

    <!-- Declare features -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- Declare permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mvp"
        android:label="@string/MVP_Camera"
        android:roundIcon="@mipmap/mvp_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVP_Camera_Theme"
        >
        <activity
            android:name="com.impostertools.mvpcamera.MainActivity"
            android:rotationAnimation="seamless">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>